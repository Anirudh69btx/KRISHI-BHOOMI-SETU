/**
 * FLIP v3.0 — useAuth Hook Re-export (Segment 01)
 * Points to unified AuthContext implementation in src/auth/useAuth.ts
 */

export { useAuth, default } from '../auth/useAuth';
export type { AuthContextType } from '../auth/AuthProvider';
